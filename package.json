{
  "name": "social-scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web:install": "npm --prefix web install",
    "web:build": "npm --prefix web run build",
    "web:dev": "npm --prefix web run dev",
    "build": "npm run web:build",
    "dev": "npm run web:build && wrangler dev",
    "deploy": "npm run web:build && wrangler deploy",
    "enqueue": "tsx src/cli/enqueue.ts",
    "queue": "tsx src/cli/queue.ts",
    "youtube-auth": "tsx src/cli/youtube-auth.ts",
    "linkedin-auth-url": "tsx src/cli/linkedin-auth-url.ts",
    "meta-auth-url": "tsx src/cli/meta-auth-url.ts",
    "pinterest-auth-url": "tsx src/cli/pinterest-auth-url.ts",
    "tiktok-auth-url": "tsx src/cli/tiktok-auth-url.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260722.1",
    "@types/node": "^22.5.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}
